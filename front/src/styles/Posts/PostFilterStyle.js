import styled from "styled-components";
import { FlexBoxCenter, LinkButton } from "../Components/CommonStyle";

export const PostFilterContainer = styled(FlexBoxCenter)``;

export const FilterButton = styled(LinkButton)`
  margin: 0 15px;
`;
